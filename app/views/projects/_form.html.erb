<%= form_for @project, url: form_url do |f| %>



  <% if @project.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@project.errors.count, "error") %> prohibited this project from being saved:</h2>

      <ul>
      <% @project.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>

     
<div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
</div><br>


  <div class="field">
    <%= f.check_box :public %>
    <%= f.label :public %>
  </div><br>



<%= f.fields_for :project_source do |source| %>
  <%= render partial: "source_form", locals: { source: source } %>
<% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
